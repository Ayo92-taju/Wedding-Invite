[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any unmatched route.
# Netlify intercepts form POSTs before this rule, so RSVP/Love Notes still submit.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
